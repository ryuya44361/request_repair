        <td><%= reservation.completion.created_at.strftime('%Y/%m/%d %H:%M') %></td>
        <td><%= reservation.engineer.last_name %> <%= reservation.completion.reservation.engineer.first_name %></td>
        <td><%= reservation.customer.last_name %> <%= reservation.customer.first_name %></td>
        <td><%= reservation.genre.name %></td>
        <td>
            機種：<%= reservation.model_number %><br />
            S/N:　<%= reservation.serial_number %>
        </td>